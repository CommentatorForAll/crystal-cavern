# Crystal Cavern NixOS Configurations

## secrets
Secrets are managed via [agenix](https://github.com/ryantm/agenix).
To edit a secret cd into the secrets directory and use `agenix -e path/to/secret.age` to open your $EDITOR.
